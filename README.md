# EasyAI

EasyAI is an AI SaaS app for creating written content, images, and document reviews from a single workspace.

This repository will hold the React frontend and the Express backend. Setup steps and a full project overview will be added as the app takes shape.

## Apps

- `frontend` : Vite + React client
- `backend` : Express API
